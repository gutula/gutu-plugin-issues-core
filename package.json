{
  "name": "gutu-plugin-issues-core",
  "private": true,
  "type": "module",
  "workspaces": [
    "framework/builtin-plugins/*"
  ],
  "scripts": {
    "build": "bun run --cwd framework/builtin-plugins/issues-core build",
    "typecheck": "bun run --cwd framework/builtin-plugins/issues-core typecheck",
    "lint": "bun run --cwd framework/builtin-plugins/issues-core lint",
    "test": "bun run --cwd framework/builtin-plugins/issues-core test",
    "test:contracts": "bun run --cwd framework/builtin-plugins/issues-core test:contracts",
    "test:integration": "bun run --cwd framework/builtin-plugins/issues-core test:integration",
    "test:migrations": "bun run --cwd framework/builtin-plugins/issues-core test:migrations",
    "test:unit": "bun run --cwd framework/builtin-plugins/issues-core test:unit",
    "docs:summary": "node scripts/docs-summary.mjs",
    "docs:check": "node scripts/docs-check.mjs"
  }
}
